export const ResourceSchemaEditorProperties = {
  /** The resource's form id / route segment — same value `ResourceTable` uses for `getFormDef`. */
  formId: { type: String, required: true as const },
};
